name: Deliberately insecure pull request workflow

on:
  pull_request_target:

permissions: write-all

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - run: echo "Untrusted pull request content checked out with write permissions"
